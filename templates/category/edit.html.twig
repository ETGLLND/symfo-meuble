{% extends "base.html.twig" %}
{% block title %}
	Editer -
	{{ category.name }}
{% endblock %}

{% block body %}
	<h1>Catégorie
		{{ category.name }}</h1>
	{{ form_start(form) }}

	{{ form_widget(form) }}

	<button class="btn btn-success mt-2">Enregistrer</button>

	{{ form_end(form) }}
{% endblock %}
