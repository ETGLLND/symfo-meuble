{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ furniture.name }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button>Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}